<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ClosingJobFrag">

    <!-- TODO: Update blank fragment layout -->
    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/imageclosedjobs"
        android:layout_width="90dp"
        android:layout_height="50dp"
        android:layout_centerHorizontal="true"
        android:scaleType="centerCrop"
        android:layout_marginTop="90dp"
        app:srcCompat="@drawable/briefcaseremove" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/txto"
        android:text="You didn't apply for any job yet!"
        android:gravity="center"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/imageclosedjobs"
        android:layout_marginTop="20dp"
        android:textColor="#000"
        android:autoSizeTextType="uniform"
        android:textAppearance="@style/Base.TextAppearance.AppCompat.Large"
        />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="Find your Job and apply for free"
        android:layout_below="@+id/txto"
        android:layout_marginTop="20dp"
        android:id="@+id/txto1"
        android:textColor="@color/darkgray"

        />
    <Button
        android:layout_width="250dp"
        android:layout_height="40dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/txto1"
        android:text="Search Jobs"
        android:textAllCaps="false"
        android:drawableLeft="@drawable/ic_menu_send"
        android:paddingLeft="5dp"
        android:paddingRight="10dp"
        android:drawablePadding="10dp"
        android:layout_marginTop="70dp"
        android:background="@drawable/button_round"
        />



</RelativeLayout>